// This file imports Swiper CSS as side effects
// It will be dynamically imported, so CSS loads only when Swiper is needed
import "swiper/css";
import "swiper/css/grid";
import "swiper/css/pagination";
